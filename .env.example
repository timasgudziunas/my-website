# Resend API key, from the Resend dashboard (Settings > API Keys)
RESEND_API_KEY=

# Resend audience ID that email signups are added to, from the Resend audience settings
RESEND_AUDIENCE_ID=

# Production site URL, used for metadataBase; falls back to http://localhost:3000 if unset
NEXT_PUBLIC_SITE_URL=
